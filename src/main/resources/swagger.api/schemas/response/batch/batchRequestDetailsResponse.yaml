description: Collection of multi-item batch request details
type: object
properties:
  batchRequestDetails:
    description: A list of batch request details
    type: array
    items:
      $ref: 'batchRequestDetail.yaml'
  totalRecords:
    description: Total number of requests
    type: integer
additionalProperties: false
required:
  - batchRequestDetails
  - totalRecords
